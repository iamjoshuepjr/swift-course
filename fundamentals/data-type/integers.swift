/* An integer data type is used to represent a whole number with no fractional component. 
   We use the Int keyword to create integer type variables. */


// It can store both positive and negative whole numbers
let birthyear: Int = 1995
let stockQuantity: Int = 1004;

// Unsigned Integers; it can olny store non-negative integers
let age: UInt = 24;